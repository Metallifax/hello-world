/**
 * This is the Main class of the Greeter exercise. In computer science, we think of
 * <a href="https://docs.oracle.com/javase/tutorial/java/javaOO/classes.html#PageTitle">classes</a>
 * as the blueprints of an object, something we'll talk more about as the track progresses.
 * @see Class
 */
public class Greeter {

    /**
     * In Java classes, we use <a href="https://docs.oracle.com/javase/tutorial/java/javaOO/methods.html#PageTitle">methods</a>
     * to create functionality inside our classes so that our objects can do useful things, like make calculations,
     * parse strings or even say "Hello, World!"
     * <p>Enter some text between the quotation marks after the {@code return} token to get started.</p>
     * @return "Hello, World!" as a {@code String}
     * @see String
     */
    public String getGreeting() {
        return "Hello, World!";
    }

}
