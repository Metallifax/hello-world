/**
 * Main class of the Greeter exercise which holds our {@code getGreeting()} method.
 */
public class Greeter {

    /**
     * In Java, we write <a href="https://docs.oracle.com/javase/tutorial/java/javaOO/methods.html#PageTitle">methods</a>
     * so that our classes can do useful things, like make calculations, parse strings or say a greeting for the world!
     * <p>Type some text between the quotation marks after the {@code return} token to get started writing Java code!</p>
     * @return "Hello, World!" as a {@code String}
     */
    public String getGreeting() {
        return "Hello, World!";
    }

}
